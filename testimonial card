import type { Testimonial } from '@/types'

interface TestimonialCardProps {
  testimonial: Testimonial
}

export default function TestimonialCard({ testimonial }: TestimonialCardProps) {
  return (
    <div className="bg-white/[0.04] border border-white/[0.08] p-8">
      <div className="flex gap-1 mb-5">
        {Array.from({ length: 5 }).map((_, i) => (
          <span key={i} className="text-[var(--accent)] text-sm">★</span>
        ))}
      </div>
      <p className="text-[0.9rem] text-white/70 leading-[1.7] italic font-serif-display mb-6">
        &ldquo;{testimonial.quote}&rdquo;
      </p>
      <div className="flex items-center gap-3">
        <div className="w-10 h-10 rounded-full bg-[var(--accent)] flex items-center justify-center font-syne font-bold text-[0.85rem] text-white flex-shrink-0">
          {testimonial.initials}
        </div>
        <div>
          <p className="font-syne font-bold text-[0.85rem] text-white">{testimonial.name}</p>
          <p className="text-[0.72rem] text-white/40">
            {testimonial.role}, {testimonial.company}
          </p>
        </div>
      </div>
    </div>
  )
}